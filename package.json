{
  "name": "shike",
  "version": "1.0.0",
  "description": "使用视觉模型估算食物热量的 Android 应用",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check public/analyze.js && node --check public/app.js && node --test",
    "check:android": "npm run sync:android && cd android && ./gradlew lintDebug testDebugUnitTest",
    "test": "node --test",
    "sync:android": "cap sync android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  },
  "overrides": {
    "brace-expansion": "5.0.9",
    "tar": "7.5.22",
    "uuid": "11.1.1"
  }
}
