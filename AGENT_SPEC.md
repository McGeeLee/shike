# 食刻 EAT APP · 智能体规范（轻量版）

> 版本：v1.0（对应当前 `server.js` 实现）
> 定位：本文件是「食刻」拍照估算热量功能的**智能体规范**——说明模型扮演什么角色、输入输出的约定、哪些事归模型、哪些事归 App、以及未来如何把"算热量"从模型手里收回到代码里。
>
> **一句话定性**：本 App 是一个**单发的视觉识别功能**（拍照 → 一次模型调用 → 返回结构化结果），**不是**会多步规划、连续调用工具的"复杂智能体"。因此本规范刻意保持精简，只规范真正需要规范的部分。

---

## 1. 角色与定位

| 项 | 内容 |
|---|---|
| 智能体名称 | 食刻营养识别助手 |
| 形态 | 单轮、单次调用的视觉→结构化输出 |
| 输入 | 一张食物照片（可选：用户文字补充，如价格、容器大小、品牌） |
| 输出 | 一个固定结构的 JSON（食物列表 + 热量 + 三大营养素 + 置信度 + 说明） |
| 不做的事 | 多轮对话、自主规划、调用外部工具、联网检索 |

> 设计原则：**模型只做"只有模型能做"的事（看图、识别、估重量）；其余确定性的事（查表、算数、汇总）应尽量交给代码。** 详见 §4。

---

## 2. 系统提示词（System Prompt）

当前线上提示词（`server.js` 中 `SYSTEM_PROMPT`），整理为结构化形式如下，便于维护与评审。**语义与现状一致**，只是排版更清晰：

```text
你是一位专业营养师。任务：根据食物照片，识别食物种类，并估算每种食物的份量、热量（千卡）与三大营养素（蛋白质、碳水、脂肪）。

【份量判断】
- 参考照片中常见餐具尺寸（碗、盘、杯）来判断份量。
- 若用户提供补充说明（价格、单价、总量、容器大小、品牌），优先据此推算份量，并在 notes 里写明推算过程。
  示例：「花14元，单价1.42元/两」→ 重量 ≈ 14 ÷ 1.42 ≈ 9.86 两 ≈ 493 克（1两=50克）。

【估算态度】
- 宁可给出合理区间的中间值，也不要拒绝估算。
- 若照片中没有可识别食物，将 is_food 设为 false，foods 留空。

【输出】
- 严格按约定的 JSON 结构输出（见 §3）。
- 所有文字使用简体中文。
```

维护约定：
- 提示词只放**通用规则与口径**，不放任何 API key、用户隐私、或一次性临时内容。
- 修改提示词前后，建议用同一批测试照片回归对比（见 §7）。

---

## 3. 输入输出契约（I/O Contract）

### 3.1 输入

| 字段 | 必填 | 说明 |
|---|---|---|
| `image` | 是 | base64 图片（前端已压缩到长边 1280px） |
| `mediaType` | 否 | 默认 `image/jpeg` |
| `description` | 否 | 用户文字补充（价格 / 容器 / 品牌等） |
| `provider` / `model` | 否 | 服务商与模型，默认 Claude |

### 3.2 输出（权威结构 = `FOOD_ANALYSIS_SCHEMA`）

```jsonc
{
  "is_food": true,                 // 照片中是否含可识别食物
  "foods": [
    {
      "name": "鸡胸肉",           // 食物名（中文）
      "portion": "约150克",       // 估计份量
      "calories": 248,             // 热量（千卡）
      "protein_g": 46.5,           // 蛋白质（克）
      "carbs_g": 0,                // 碳水（克）
      "fat_g": 5.4                 // 脂肪（克）
    }
  ],
  "total_calories": 248,           // 总热量
  "confidence": "medium",          // 置信度：low | medium | high
  "notes": "份量按常见餐盘推算"     // 简短说明
}
```

口径约束：
- **Claude**：用官方 `json_schema` 结构化输出强制保证结构。
- **其他服务商**（OpenAI 兼容）：用提示词约束 + 宽松解析（`parseJsonLoose`）兜底。
- **统一归一化**（`normalizeResult`）：缺字段补默认值、数字非法置 0、`total_calories` 缺失时由各项求和兜底。→ **前端永远拿到同一形状的数据，不需关心服务商差异。**

---

## 4. 职责边界：模型 / App / 服务商（本规范核心）

| 职责 | 现状 v1 | 规划 v2 | 理由 |
|---|---|---|---|
| 识别食物种类 | 模型 | 模型 | 只有视觉模型能做 |
| 估计份量 / 重量 | 模型 | 模型 | 需要看图判断 |
| **由重量算热量/营养素** | **模型"猜"** ⚠️ | **App 查知识库 + 计算** ✅ | 查表+乘法是确定性运算，交给代码才能稳定、可复算、省 token |
| 价格 → 重量推算 | 模型（提示词内） | 可下沉为 App 计算 | 纯算术 |
| 当日汇总 / 剩余 / 进度 | 前端（localStorage） | 不变 | 已经是代码，正确 |
| 数据持久化 | 浏览器本地 | 不变 | 隐私友好 |

> **给 boss 的要点**：当前热量数字是模型估的，同一张图多次识别可能略有出入；这是模型的固有特性。v2 把"营养数据"做成知识库、把"算热量"做成 App 里的方法（并通过工具供模型调用），模型只回「食物 + 克数」，App 查表计算——**更准、可解释、且更省成本**。这正是"在 App 做方法、通过 tool 给模型调用"思路的**正确落点**。

---

## 5. 知识库（营养参考表 · 每 100 克可食部）

> 用途：v2 由 **App 端计算**时查询；v1 阶段作为口径参考 / 评测基准。数值为常见近似值，可按权威食物成分表（如《中国食物成分表》）校准。

| 食物 | 热量(kcal) | 蛋白(g) | 碳水(g) | 脂肪(g) |
|---|---|---|---|---|
| 米饭（熟） | 116 | 2.6 | 25.9 | 0.3 |
| 白粥 | 46 | 1.1 | 9.8 | 0.2 |
| 馒头 | 221 | 7.0 | 47.0 | 1.1 |
| 面条（煮） | 110 | 3.5 | 21.0 | 0.7 |
| 鸡胸肉（熟） | 165 | 31.0 | 0 | 3.6 |
| 鸡蛋 | 144 | 13.0 | 1.1 | 9.5 |
| 牛奶（全脂，每100ml） | 64 | 3.3 | 5.0 | 3.6 |
| 瘦牛肉（熟） | 250 | 26.0 | 0 | 15.0 |
| 五花肉 | 508 | 9.0 | 0 | 53.0 |
| 三文鱼 | 208 | 20.0 | 0 | 13.0 |
| 豆腐（老） | 98 | 8.1 | 3.4 | 4.8 |
| 西兰花（熟） | 35 | 2.4 | 7.0 | 0.4 |
| 苹果 | 52 | 0.3 | 14.0 | 0.2 |
| 香蕉 | 89 | 1.1 | 23.0 | 0.3 |
| 可乐（每100ml） | 43 | 0 | 11.0 | 0 |

> 说明：表为**起步样例**，建议优先收录**用户高频食物**，无需追求大而全（小 App 不需要万条食物库）。未命中时回退到模型估算或品类默认值。

---

## 6. 计算逻辑（确定性规则）

这些是**应当用代码实现**的固定公式（部分 v1 已在前端 / 提示词中）：

1. **热量自洽校验（Atwater 公式）**
   `kcal ≈ 蛋白(g)×4 + 碳水(g)×4 + 脂肪(g)×9`
   用途：校验模型返回是否离谱；v2 中可直接由营养素反算热量。

2. **重量推算（价格法）**
   `重量(两) = 花费金额 ÷ 单价(元/两)`；`克 = 两 × 50`
   （v1 在提示词内，v2 可下沉为 App 函数）

3. **单项营养计算（v2 主力）**
   `某项 = round( 份量克数 ÷ 100 × 知识库[食物].该项每100克值 )`

4. **当日汇总（前端，已实现）**
   `当日总热量 = Σ 各条记录`；`剩余 = 目标 − 当日总热量`；`进度 = 当日总热量 ÷ 目标`
   （默认目标 2000 kcal，可设置）

---

## 7. 质量与评测（轻量）

- 维护一组**固定测试照片**（含：正常餐、空盘 / 无食物、带价格说明、混合多菜），改提示词或换模型后跑一遍对比。
- 关注三件事：① JSON 是否始终可解析；② `is_food` 误判率；③ 热量估值是否在合理区间。
- 不必引入复杂评测框架——这个体量手工抽查即可。

---

## 8. 安全与边界

- **密钥**：API key 来自用户设置或服务端环境变量，**绝不写入提示词或前端代码**（现状已正确）。
- **隐私**：照片仅转发给用户自选的服务商，数据存浏览器本地，不入库。
- **免责**：结果为**估算**，非医疗 / 营养专业建议；UI 应有提示，避免用户当作精确医学数据。
- **失败兜底**：无食物 → `is_food=false`；解析失败 / 服务商报错 → 返回明确中文错误，不返回脏数据。

---

## 9. 版本演进

- **v1（当前）**：单次调用，模型负责识别+估算全部数值；结构化输出 + 归一化兜底。
- **v2（规划，可选）**：引入 §5 知识库 + §6 计算逻辑，新增工具
  `lookup_nutrition(food_name: string, grams: number) → {calories, protein_g, carbs_g, fat_g, matched_name}`；
  模型只回「食物 + 克数」，App 查表计算。**更准、可解释、更省 token。**
- **是否升级 v2 的判断**：若用户在意数值稳定性 / 想做历史统计 → 值得做；若只是个人随手记录、当前精度已够用 → v1 维持即可，不必为升级而升级。

---

*本规范与 4M 体量的 App 相匹配：只规范"提示词口径、输入输出契约、模型/代码分工"这三件真正重要的事，不引入与体量不符的重型框架。*
